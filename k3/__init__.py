"""k3 — client presets for the K3 inference engine.

A preset bundles everything a coding agent needs to talk to K3 correctly:
route + dialect, tool-call parser/emitter, chat template, reasoning
translation (both directions), sampling defaults, and model aliasing.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
